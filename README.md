# string-of-numbers-deflate
Compression tool for deflate strings represent comma separated numbers to shorter ASCII string (base64).

### Example
input string
"1,300,237,188,26,3,4,1,256" // 26 length
can be compressed to
"AQACARaiMRMs" // 12 length
